# Talking Orange AR Project - System Dependencies
# Install with: sudo apt-get install -y $(cat requirements.txt)

# Python runtime and tools
python3>=3.8
python3-pip
python3-venv
python3-dev

# Development tools
git
curl
wget
build-essential

# Audio processing (for voice features)
ffmpeg
libasound2-dev
libsndfile1-dev
libpulse-dev
portaudio19-dev
python3-pyaudio

# TTS engines
espeak
festival
libttspico-utils

# Whisper dependencies
libssl-dev
libffi-dev

# Image processing (for QR codes and image assets)
imagemagick
libmagick++-dev

# Database
sqlite3

# Web development tools
nginx

# Node.js (for frontend development)
# Install via: curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash - && sudo apt-get install -y nodejs

# 3D Development
# Blender (install via snap or download from blender.org)
# sudo snap install blender --classic

# Optional: Docker for containerized development
# docker.io
# docker-compose
