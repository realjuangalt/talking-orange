# Talking Orange AR Project - Python Dependencies
# Install with: pip3 install -r requirements-python.txt
# 
# This file contains ONLY Python packages actually used in the codebase.
# System dependencies (apt packages) are installed directly by install.sh
#
# NOTE: AR functionality (A-Frame, MindAR) uses pre-bundled JavaScript libraries
# in frontend/lib/ - no Python packages needed for AR. The backend only serves
# static files and handles voice processing API endpoints.

# Flask web framework
Flask>=2.3.0
Flask-CORS>=4.0.0

# Environment configuration
python-dotenv>=1.0.0

# HTTP requests and async
requests>=2.28.0
aiohttp>=3.8.0

# Voice processing (Whisper for speech-to-text)
openai-whisper>=20231117
torch>=1.13.0
torchaudio>=0.13.0
pydub>=0.25.1

# TTS (Text-to-Speech)
gtts>=2.3.0

# Audio processing
soundfile>=0.12.1

# Data processing (required by torch/whisper)
numpy>=1.21.0

# Schema validation
jsonschema>=4.0.0

# Image processing (for AR target validation)
Pillow>=9.0.0

# Note: MindAR compiler uses Node.js with 'mind-ar' npm package
# Install with: npm install mind-ar
# Same compiler as: https://hiukim.github.io/mind-ar-js-doc/tools/compile
